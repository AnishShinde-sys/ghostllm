[project]
name = "ghostllm"
version = "0.1.0"
description = "Synthetic conversation pipeline for personality distillation"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.55.0",
    "anthropic>=0.40.0",
    "python-dotenv>=1.0.1",
]
